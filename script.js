// Get a reference to the button element
const button = document.getElementById('constructionButton');

// Add a click event listener to the button
button.addEventListener('click', () => {
    const message = "Feature under construction. For any queries Mail - avishekojha66@gmail.com";
    alert(message);
});
